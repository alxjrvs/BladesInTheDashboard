
<div id="healing-clock">
  <%= render(
    CustomCheckboxListComponent.new(
      size: 4,
      model: player_character,
      comparitor: player_character.healing_clock,
      field: :healing_clock,
      classes: "xp")
      ) %>
</div>
