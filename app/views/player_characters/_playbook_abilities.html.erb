<div id="playbook-abilities" class="cell col-4">
  <div class="info-cell">
    <h2>Playbook Abilities
      <div class="xp-row">
        <%- 8.times do |i| %>
          <% checked = i + 1<= player_character.playbook_xp %>
          <% value = checked ? i :  i + 1 %>
          <%= render 'shared/input_box',
                        field: :playbook_xp,
                        model: player_character,
                        value:,
                        checked:,
                        classes: "xp"
                      %>
        <% end %>
      </div>
    </h2>
  </div>
</div>
