<% classes ||= '' %>
<% disabled ||= false %>
<%= form_with(model: model, class: 'input') do |form| %>
  <%= form.select field,
    collection,
    {},
    onchange: 'this.form.requestSubmit()',
    class: "select #{classes}",
    disabled:
  %>
<% end %>
