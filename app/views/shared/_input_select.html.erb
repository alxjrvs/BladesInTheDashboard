<% model ||= [@game, player_character] %>


<%= form_with(model: model, class: 'input') do |form| %>
  <%= form.select field, collection, {}, onchange: 'this.form.submit()', class: 'select'  %>
<% end %>
