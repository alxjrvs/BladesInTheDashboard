<% classes ||= '' %>
<% disabled ||= false %>
<%= form_with(model: model, class: 'custom-input-container') do |form| %>
  <%= form.select field,
    collection,
    {},
    onchange: 'this.form.requestSubmit()',
    class: "custom-select #{classes}",
    disabled:
  %>
<% end %>
